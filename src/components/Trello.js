import { Button, Card, CardBody, CardHeader, Col, Input } from 'reactstrap';

function Trello() {
    return (
        <Card>
            <CardBody></CardBody>
        </Card>
    );
}

export default Trello;
